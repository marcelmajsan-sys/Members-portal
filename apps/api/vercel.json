{
  "installCommand": "cd ../.. && pnpm install && cd apps/api && npx prisma generate --schema=../../packages/db/prisma/schema.prisma",
  "buildCommand": "cd ../.. && pnpm turbo build --filter=@ecommerce-hr/api",
  "outputDirectory": ".",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
